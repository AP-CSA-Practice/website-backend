spring.application.name=ap-csa-practice

# MongoDB Configuration for tests
# This will be overridden by environment variables in Docker
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/apcsadb_test}

# Disable auto-index creation during tests to improve startup time
spring.data.mongodb.auto-index-creation=false