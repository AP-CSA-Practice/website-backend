spring.application.name=ap-csa-practice

# MongoDB Configuration
# Connection URI will be used instead of host/port
# If your MongoDB requires authentication, include username and password in the URI, for example:
# mongodb://username:password@host:port/database
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/apcsadb}